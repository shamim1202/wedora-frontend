import { Link } from "react-router";

const Logo = () => {
  return (
    <Link className="text-xl md:text-3xl font-bold text-gray-800 dark:text-white font-playfair hover:text-primary transition-colors cursor-pointer">
      Wedora
    </Link>
  );
};

export default Logo;
